# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
# .terraform.lock.hcl deve ser versionado — fixa as versões do provider.

# Segredos e ambiente
.env
!.env.example

# Sistema
.DS_Store

# intermediários da gravação — o MP4 e o GIF finais ficam em _bmad-output/
tools/video/
tools/capturas/*.png

# Rascunho do dossiê renderizado a partir do .md — o entregável sai do Claude Design.
_bmad-output/implementation-artifacts/prompt-claude-design-modernizacao.pdf
