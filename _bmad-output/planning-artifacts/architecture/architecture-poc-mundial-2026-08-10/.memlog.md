---
scope: Modernização do sistema Mundial - Conferência (legado Visual FoxPro dun14.SCX) para stack .NET + Dapper + Angular + SQL Server, 6 entidades, fluxo de conferência de recebimento
purpose: build-substrate
altitude: feature
updated: 2026-08-10T20:01
---

- (version) .NET 10 LTS — lançado 11/11/2025, suporte até 14/11/2028. Verificado em dotnet.microsoft.com e devblogs.microsoft.com. Escolhido sobre .NET 8 (LTS expira nov/2026).
- (version) Dapper 2.1.79 — última versão em 16/05/2026 (nuget.org). Target netstandard2.0, compatível com .NET 10.
- (version) Angular 22.1.1 — release 03/06/2026, versão ativa em ago/2026. Angular 21 em LTS até maio/2027. Escolhido 22 por ser greenfield.
- (version) DbUp escolhido para migrations sobre FluentMigrator: abordagem SQL-first, DBA lê o script literal. Encaixa em migração de legado com PK composta e schema fiel ao DBF.
- (version) SQL Server 2022 — imagem mcr.microsoft.com/mssql/server:2022-latest.
- (constraint) Diretiva do autor (bmad-context.md, VERBATIM): 'A aplicação é toda em Português do Brasil'.
- (constraint) Diretiva por componente (frontend/backend/database, VERBATIM): 'O modelo de dados fidedigno ao RNC UIR'.
- (constraint) Precedência fixada pelo pacote RNC: diretiva do autor > canvas > derivado > default.
- (constraint) Fonte de verdade do modelo: UIR do workspace RNC b2e707a2-7df1-4156-b9ad-a35ed33e2e78, módulo dun14.SCX. 6 entidades, 70 regras, 23 integrações, 4 telas.
- (constraint) Legado guarda senha em texto puro (usuario.senha VARCHAR; comparação This.Value#senha, RK do FR-26). UIR marca 3 achados CLEARTEXT_PASSWORD HIGH. Replicar é inaceitável.
- (decision) AD-1 Paradigma: Ports & Adapters (hexagonal) com fatias verticais por caso de uso. Binds: todo o backend. Prevents: regra de negócio vazando para controller ou SQL. Rule: Domain não referencia Infrastructure nem Api; dependências apontam para dentro.
- (decision) AD-2 Identidade: cada entidade legada mantém a chave natural do UIR como UNIQUE e ganha surrogate 'id' INT IDENTITY para a API. Prevents: perda da identidade de negócio (conferencia tem PK composta de 6 colunas no UIR) e rotas REST impossíveis.
- (decision) AD-3 Fidelidade ao UIR: nome de tabela e de coluna vêm do getErModel, sem renomear. Campo de tela nunca vira coluna. Prevents: colunas fantasma (dataenvironment, timer1, listnf, listprod, codigo1) que o pacote BMAD inventou.
- (decision) AD-4 Mapeamento label→coluna: dataBindings do UIR é a autoridade. rec_nf.documento→conferencia.acesso; rec_nf.codigo (label 'Código Ean 13:')→conferencia.dun14. Prevents: gravar EAN-13 na coluna do DUN-14.
- (decision) AD-5 Rastreabilidade de regra: toda regra implementada cita seu ruleKey RK-<12hex> em atributo/XMLdoc. Prevents: regra perdida ou duplicada; permite verificação via getRule do MCP RNC.
- (decision) AD-6 Classificação de regra por camada: DOMAIN_INVARIANT→Domain; APPLICATION_VALIDATION e VALIDATION→Application; confirmação de MessageBox→UI (9 regras, não são regra de servidor); geração ZPL→serviço de impressão (7 regras). Prevents: 16 das 70 regras virarem validação de API por engano.
- (decision) AD-7 Senha: nunca em claro. PasswordHasher do ASP.NET Core Identity. Senha legada NÃO migra — reset obrigatório no primeiro acesso. Prevents: replicar os 3 achados CLEARTEXT_PASSWORD HIGH do UIR.
- (decision) AD-8 Autorização: tabela acesso(matric, arquivo) vira policy-based authorization; as 4 flags (alterar, incluir, excluir, consultar) são permissões por tela. Prevents: cada tela inventar seu próprio controle de acesso.
- (decision) AD-9 Persistência: Dapper com SQL explícito, um repositório por agregado, sem change tracking. Migrations em DbUp, SQL literal versionado. Prevents: dois estilos de acesso a dado convivendo.
- (decision) AD-10 Agregado Conferencia: finalizar é transição de estado atômica (fechado, matr_fec, dt_hora, situacao) em transação única. Prevents: conferência meio-fechada; dois caminhos de mutação do mesmo estado.
- (decision) AD-11 Contrato de erro: RFC 9457 application/problem+json, com extensão ruleKey quando o erro vem de uma regra. Prevents: cada endpoint inventando formato de erro.
- (decision) AD-12 Idioma: identificadores de tabela/coluna preservados do legado; tipos de domínio e rotas em português; toda mensagem ao usuário em pt-BR. Prevents: metade do código em inglês e metade em português.
- (decision) AD-13 Frontend Angular 22: standalone components e signals, sem NgModules. Um feature folder por entidade + um pelo fluxo de conferência. Prevents: mistura de estilo NgModule/standalone.
- (decision) AD-14 Integração estok_sgm (ODBC, logicOpaque no UIR): isolada atrás de um port com anticorruption layer, desligada por feature flag na Fase 1. Prevents: acoplar o núcleo novo a um sistema externo não mapeado.
- (assumption) [ASSUMPTION] log_even (0 colunas no UIR) vira tabela de auditoria append-only com schema novo — o UIR não recuperou as colunas.
- (assumption) [ASSUMPTION] A impressora Zebra fala ZPL por socket TCP 9100. O UIR só mostra a montagem da string ZPL, não o transporte.
- (assumption) [ASSUMPTION] barr_emb3 existe em estoq — aparece em 3 regras (RK-9f4468b42859, RK-75e2169fe930, RK-dfe2ca45ec1a) mas o getErModel lista só 6 colunas sem ele.
- (assumption) [ASSUMPTION] A tabela 'entrada' (5 SQL_PASSTHROUGH no UIR) e o módulo esco_imp pertencem ao ERP externo, fora do escopo da Fase 1.
- (question) Tipos das colunas de estoq vêm como UNKNOWN no getErModel — precisa inspeção do DBF ou decisão do cliente.
- (question) Migração de dado do legado está fora de escopo na Fase 1 do brief. Confirmar se continua fora.
- (direction) Decidido com o usuário em 2026-08-10: backend .NET + Dapper; escopo de 6 entidades + login; fluxo de conferência (bipar→resolver DUN-14→acumular→finalizar→ZPL) entra como story própria.
- (event) Reviewer Gate executado: lint_spine (5 achados low, corrigidos) + 3 lentes sequenciais (versões, adversarial, rubrica). Reviews em reviews/.
- (version) CORREÇÃO do gate: TypeScript 6.x, não 5.9 — Angular 22 não aceita 5.9 ou anterior (angular.dev/reference/versions). Erro que quebraria o build.
- (version) CORREÇÃO do gate: Node.js 24 LTS (Active) em vez de 22 (Maintenance desde 2026). dbup-sqlserver 7.2.0, FluentValidation 12.1.1, Microsoft.Data.SqlClient 7.0.2 — versões reais do NuGet, antes asseridas de memória.
- (decision) SQL Server 2022 mantido apesar de 2025 estar GA desde 18/11/2025. Razão: boring technology — o risco deste projeto está na tradução do FoxPro, não na versão do banco. Nada no spine usa recurso do 2025; trocar é uma linha no compose. Registrado em Deferred.
- (decision) AD-3 ganhou duas exceções após achado adversarial A-1/A-2: (1) segurança vence fidelidade, autorizando senha->senha_hash do AD-7; (2) coluna citada por regra mas ausente do getErModel entra marcada com o ruleKey — caso estoq.barr_emb3.
- (decision) AD-15 Contrato único de listagem: ?pagina&tamanho&busca&ordem, envelope {itens,total,pagina,tamanho}. Prevents: cada entidade paginar diferente e o frontend manter um cliente por recurso.
- (decision) AD-16 Um dono de escrita por tabela. estoq é escrito só pelo cadastro DUN-14; o fluxo de conferência lê via port read-only. Prevents: dois repositórios escrevendo a mesma tabela com regras de unicidade valendo de um lado só.
- (decision) AD-17 qtd_rec acumula (soma, nunca substitui) + rowversion com 409 Conflict. Prevents: dois operadores no mesmo documento com last-write-wins apagando contagem.
- (decision) AD-18 Servidor é autoridade de validação; cliente espelha só regra de forma. Regra que consulta banco (_tally>0, Reccount()=0) nunca é reimplementada em TypeScript.
- (decision) AD-19 Instante em UTC no banco e na API; exibição em America/Sao_Paulo via TZ_APLICACAO. Nenhum DateTime.Now — só o port IRelogio.
- (decision) AD-20 Todo ruleKey tem teste citando o mesmo ruleKey no nome; relatório de rastreabilidade ruleKey->teste a cada build. Fecha a dimensão de teste, que estava em silêncio total.
- (question) docs/ux/DESIGN.md precisa ser refeito a partir de dataBindings do UIR — hoje trata label de tela como nome de coluna (D-03 do relatório de divergência).
- (event) spine finalizado
- (event) UPDATE do spine após leitura da fonte legada via MCP (getSourceFile, 11 arquivos RETAINED). 5 ADs emendados no lugar, 1 novo. IDs estáveis, nada renumerado. Lint limpo, 21 ADs.
- (decision) AD-3 emendado: getErModel deixa de ser autoridade do schema — provou-se incompleto (6 de 116 colunas de estoq, todas UNKNOWN; log_even com zero colunas). Nova ordem: DDL SQL Server > estoq_structure.TXT > reg_log no PRG > MCP. Ganhou a exceção que faltava para id e rowversion, que o AD-2 e o AD-17 exigiam e o AD-3 proibia.
- (decision) AD-8 emendado: acesso.arquivo é nome de TABELA (char(10)), não de tela. Fonte: readme.txt do cliente e DDL. Uma tela que toca N tabelas exige N permissões.
- (decision) AD-10 emendado: o agregado é o DOCUMENTO (filial+orig_des+tipo_doc+SERIE+numero), não a linha. A PK inclui codigo, logo cada linha é um item da nota. Finalizar age em todas as linhas de uma vez. O sistema nunca cria linha de conferencia — elas vêm da integração da nota fiscal.
- (decision) AD-14 emendado: estok_sgm é o próprio banco sgm, onde acesso/conferencia/forne/usuario já vivem em SQL Server. Não há ERP externo a isolar. esco_imp é 'escolhe impressora', não importação de estoque. A integração real é a nota fiscal.
- (decision) AD-17 emendado: qtd_rec SUBSTITUI com confirmação, não acumula. Base: campo escalar de item, sem tabela onde acumular, e aviso que só protege se a ação for destrutiva. Set Exclusive On no legado enfraquece a hipótese de acúmulo concorrente. Sem prova direta — .SCX binário não retido.
- (decision) AD-21 novo: andaime de demo em projeto Mundial.Demo removível, atrás de MODO_DEMO. Seed nunca em migration DbUp — migration é schema, seed é dado de demo. Nenhum tipo de Dominio ou Aplicacao conhece o projeto.
- (constraint) Tipos reais da fonte adicionados ao spine: CODBARR* char(13) = EAN-13, BARR_EMB* char(14) = DUN-14, CODIGO char(5), EMBALQT numeric(9,4), conferencia.acesso char(25), situacao char(1), QTD_* decimal(10,3). Largura é contrato — código de barras com um caractere a mais deixa de casar.
